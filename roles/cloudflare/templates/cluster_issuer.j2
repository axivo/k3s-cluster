apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ cloudflare_resources.cluster.issuer }}
spec:
  acme:
    email: {{ cluster_vars.service.postfix.user.alias }}
    privateKeySecretRef:
      name: {{ cloudflare_map.tls.key.prefix }}-key
    server: {{ cloudflare_resources.acme.server.url }}
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              key: {{ cloudflare_vars.kubernetes.api.token.key }}
              name: {{ cloudflare_vars.kubernetes.api.token.name }}
