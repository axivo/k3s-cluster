apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: {{ cilium_vars.kubernetes.loadbalancer.ip_pool.name }}
  namespace: {{ cilium_vars.kubernetes.namespace }}
spec:
  blocks:
    - start: {{ global_map.infrastructure[role_name].loadbalancer.ip_pool.blocks.start }}
      stop: {{ global_map.infrastructure[role_name].loadbalancer.ip_pool.blocks.stop }}
